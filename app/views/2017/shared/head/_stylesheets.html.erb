<%= stylesheet_link_tag Current.theme, media: "all" unless lite_mode? %>

<style>
  <% if @article.present? && @article.css.present? %>
    <%= @article.css %>
  <% end %>

  <% if @page.present? && @page.css.present? %>
    <%= @page.css %>
  <% end %>
</style>

<%= render_themed "shared/head/lite_mode_css" if lite_mode? %>
